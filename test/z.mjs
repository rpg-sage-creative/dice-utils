import { debug, info, warn, error } from "@rsc-utils/console-utils";
import { assert, runTests, startAsserting, stopAsserting } from "@rsc-utils/test-utils";

runTests(async function testDiceUtils() {
	error(`create DiceTest tests`);
	warn(`write DiceThreshold logic`);
	error(`create DiceThreshold tests`);
	error(`create ExplodeDice tests`);
}, true);