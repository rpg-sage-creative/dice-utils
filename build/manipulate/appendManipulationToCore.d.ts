import type { TokenData } from "@rsc-utils/string-utils";
import type { DicePartCore } from "../dice/DicePart.js";
export declare function appendManipulationToCore(core: DicePartCore, token: TokenData, index: number, tokens: TokenData[]): boolean;
